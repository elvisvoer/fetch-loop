{
  "name": "fetch-loop",
  "author": {
    "name": "Elvis Adomnica",
    "url": "erranto.com"
  },
  "description": "Simple abstraction over fetch to only permit a certain amount of requests per time interval.",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "files": [
    "dist"
  ],
  "main": "./dist/fetch-loop.umd.cjs",
  "module": "./dist/fetch-loop.js",
  "exports": {
    ".": {
      "import": "./dist/fetch-loop.js",
      "require": "./dist/fetch-loop.umd.cjs"
    }
  },
  "scripts": {
    "build": "tsc && vite build"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
